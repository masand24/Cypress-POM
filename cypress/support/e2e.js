

// Import commands.js using ES2015 syntax:
import './commands'

// Alternatively you can use CommonJS syntax:
// require('./commands')
 

// if we are using cy.xpath instead of cy.get for xpath we need to provide below command globally here.
require('cypress-xpath')